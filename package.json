{
  "name": "cypress-sample",
  "version": "1.0.0",
  "repository": "https://github.com/yasu-s/cypress-sample.git",
  "license": "MIT",
  "scripts": {
    "start": "yarn && lite-server --baseDir=public",
    "cypress:open": "yarn && cypress open"
  },
  "devDependencies": {
    "cypress": "^4.2.0",
    "lite-server": "^2.5.4"
  }
}
